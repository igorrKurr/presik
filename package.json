{
  "name": "presik",
  "version": "4.6.1",
  "private": true,
  "description": "Live in-class quizzes for interactive lectures: students answer from their phones, the projector shows the question and result, and arrow keys drive your slides and the quiz in sync. Bring any deck — Marp, PDF, PowerPoint, or Keynote — and presik renders it with the quiz interleaved, in class or over Zoom. Runs locally; the engine is separate from content (courses/slides/questions).",
  "main": "src/server.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/igorrKurr/presik.git"
  },
  "bin": {
    "presik": "bin/cli.js",
    "presik-report": "bin/report.js"
  },
  "files": [
    "src/",
    "bin/",
    "public/",
    "templates/",
    "packages/widget-sdk/index.js",
    "CONVENTIONS.md",
    "UPGRADE.md"
  ],
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "tunnel": "node src/server.js --tunnel",
    "report": "node src/report.js",
    "test": "node --test",
    "build": "node build/build.js",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "optionalDependencies": {
    "@marp-team/marp-cli": "4.4.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "pdfjs-dist": "^6.1.200",
    "postject": "^1.0.0-alpha.6"
  }
}
